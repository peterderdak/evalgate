{
  "name": "@evalgate/worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@evalgate/eval-core": "workspace:*",
    "@evalgate/shared": "workspace:*",
    "tsx": "^4.19.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10"
  }
}
