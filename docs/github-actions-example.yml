name: EvalGate CLI Example

on:
  workflow_dispatch:
  pull_request:

jobs:
  evalgate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.6.3
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - name: Run EvalGate gate
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          pnpm evalgate run \
            --dataset ./datasets/sample-support-tickets.jsonl \
            --config ./docs/ticket-triage.evalgate.json \
            --provider openai \
            --model gpt-4.1-mini \
            --fail-on-gate
